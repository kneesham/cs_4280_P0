#include <stdio.h>
#include <stdlib.h>
#include "node.h"
#include "util.h"

node_t * buildTree(FILE * file) {
    node_t * root = NULL;
    printf("text from file is: %s\n", getTextFromFile(file));

    return root;
}